/**
 * Maintenance Mode Configuration
 * 
 * Set IS_UNDER_MAINTENANCE to true to display the Maintenance Page on deployed production environments.
 * Local development (localhost / 127.0.0.1) will ALWAYS bypass maintenance mode and run normally.
 * 
 * To turn off maintenance mode on production when ready, set IS_UNDER_MAINTENANCE to false.
 */
export const IS_UNDER_MAINTENANCE = true;
